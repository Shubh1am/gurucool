apiVersion: v1
kind: Service
metadata:
  name: gharuka-guru-backend
  namespace: gharuka-guru
spec:
  selector:
    app: gharuka-guru-backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
